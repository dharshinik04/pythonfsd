def greetings(name="Dharshu"):
    return f" {name}"

    def perform(val1,val2):
        return f" is perform: {a}+{b}={a/b}"
        def function():
            return (greetings(perform(6,1)))
            print (function())